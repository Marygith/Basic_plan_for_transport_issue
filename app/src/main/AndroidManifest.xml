<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.itmo.firstproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SouthEast"></activity>
        <activity android:name=".SouthWest" />
        <activity android:name=".NorthEast" />
        <activity android:name=".NorthWest" />
        <activity android:name=".MinCostActivity" />
        <activity android:name=".FifthActivity" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".ForthActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>